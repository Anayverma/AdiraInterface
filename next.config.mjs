/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['shorturl.at',"media.licdn.com","encrypted-tbn0.gstatic.com"],
    },
  };
  
  export default nextConfig;
  